# Pinned dependencies for RunPod serverless prebuilt image
# These versions chosen for compatibility with base image runpod/pytorch:2.4.0-py3.11-cuda12.4.1
# Adjust only if handler logs show specific incompatibilities.
torch==2.4.0
torchvision==0.19.0
# Core diffusion stack
diffusers==0.31.0
transformers==4.45.0
accelerate==1.2.0
safetensors==0.4.3
xformers==0.0.28.post1
controlnet-aux==0.0.8
# Imaging / media
opencv-python-headless==4.10.0.84
Pillow==10.4.0
imageio==2.34.0
imageio-ffmpeg==0.4.9
# Utilities / config
einops==0.8.0
omegaconf==2.3.0
numpy==1.26.4
tqdm==4.66.5
runpod==1.7.4
python-dotenv==1.0.1
# Optional model repos (git installs)
# CLIP used by some diffusion pipelines
git+https://github.com/openai/CLIP.git
# IP-Adapter (leave even if not immediately used for extensibility)
git+https://github.com/tencent-ailab/IP-Adapter.git
